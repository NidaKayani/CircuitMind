# CircuitMind API — Vercel serverless dependencies
# Scoped down from the root requirements.txt: no streamlit, no CV extras.
# Vercel's @vercel/python builder invokes the ASGI `app` directly, so uvicorn
# isn't needed here (local dev still uses `uvicorn api.app:app --reload`
# against the root requirements.txt).

fastapi>=0.111.0
pydantic>=2.0
python-multipart>=0.0.9
slowapi>=0.1.9
groq>=0.4.0
python-dotenv>=1.0.0
schemdraw>=0.19
redis>=5.0.0
